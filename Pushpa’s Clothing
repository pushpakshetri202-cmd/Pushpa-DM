<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ChromaStyle | Vibrant Fashion</title>
    <!-- Google Fonts -->
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600;800&display=swap" rel="stylesheet">
    <!-- Font Awesome for Icons -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    
    <style>
        :root {
            --primary: #FF0080;
            --secondary: #7928CA;
            --accent: #FF005C;
            --yellow: #F9CB28;
            --cyan: #00D2FF;
            --dark: #1a1a1a;
            --light: #f8f9fa;
            --gradient-main: linear-gradient(135deg, #FF0080 0%, #7928CA 100%);
            --gradient-text: linear-gradient(to right, #FF0080, #7928CA, #FF005C);
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Poppins', sans-serif;
        }

        body {
            background-color: var(--light);
            color: var(--dark);
            overflow-x: hidden;
        }

        /* --- Animations --- */
        @keyframes float {
            0% { transform: translateY(0px); }
            50% { transform: translateY(-10px); }
            100% { transform: translateY(0px); }
        }

        @keyframes pulse-glow {
            0% { box-shadow: 0 0 0 0 rgba(255, 0, 128, 0.7); }
            70% { box-shadow: 0 0 0 15px rgba(255, 0, 128, 0); }
            100% { box-shadow: 0 0 0 0 rgba(255, 0, 128, 0); }
        }

        /* --- Header --- */
        header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 1.5rem 5%;
            background: rgba(255, 255, 255, 0.95);
            backdrop-filter: blur(10px);
            position: sticky;
            top: 0;
            z-index: 1000;
            box-shadow: 0 4px 30px rgba(0, 0, 0, 0.05);
        }

        .logo {
            font-size: 1.8rem;
            font-weight: 800;
            text-transform: uppercase;
            letter-spacing: -1px;
            background: var(--gradient-text);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
        }

        nav ul {
            display: flex;
            gap: 2rem;
            list-style: none;
        }

        nav a {
            text-decoration: none;
            color: var(--dark);
            font-weight: 600;
            position: relative;
            transition: 0.3s;
        }

        nav a::after {
            content: '';
            position: absolute;
            width: 0;
            height: 3px;
            bottom: -5px;
            left: 0;
            background: var(--gradient-main);
            transition: width 0.3s;
        }

        nav a:hover::after {
            width: 100%;
        }

        .cart-icon {
            position: relative;
            font-size: 1.5rem;
            cursor: pointer;
            color: var(--secondary);
            transition: transform 0.2s;
        }

        .cart-icon:hover {
            transform: scale(1.1);
        }

        .cart-count {
            position: absolute;
            top: -8px;
            right: -8px;
            background: var(--yellow);
            color: var(--dark);
            font-size: 0.7rem;
            font-weight: 800;
            width: 20px;
            height: 20px;
            border-radius: 50%;
            display: flex;
            justify-content: center;
            align-items: center;
        }

        /* --- Hero Section --- */
        .hero {
            min-height: 80vh;
            display: flex;
            align-items: center;
            justify-content: space-between;
            padding: 0 5%;
            position: relative;
            overflow: hidden;
            background: #fff;
        }

        .hero::before {
            content: '';
            position: absolute;
            width: 600px;
            height: 600px;
            background: var(--gradient-main);
            border-radius: 50%;
            top: -100px;
            right: -100px;
            opacity: 0.1;
            filter: blur(80px);
            z-index: 0;
        }

        .hero::after {
            content: '';
            position: absolute;
            width: 400px;
            height: 400px;
            background: var(--cyan);
            border-radius: 50%;
            bottom: 50px;
            left: -100px;
            opacity: 0.1;
            filter: blur(60px);
            z-index: 0;
        }

        .hero-text {
            flex: 1;
            z-index: 1;
            padding-right: 2rem;
        }

        .hero h1 {
            font-size: 4.5rem;
            line-height: 1.1;
            margin-bottom: 1.5rem;
            color: var(--dark);
        }

        .hero h1 span {
            color: var(--primary);
            position: relative;
        }

        .hero h1 span::before {
            content: '';
            position: absolute;
            width: 100%;
            height: 15px;
            bottom: 5px;
            left: 0;
            background: var(--yellow);
            z-index: -1;
            transform: rotate(-2deg);
            opacity: 0.5;
        }

        .hero p {
            font-size: 1.2rem;
            color: #666;
            margin-bottom: 2rem;
            max-width: 500px;
        }

        .btn {
            padding: 1rem 2.5rem;
            background: var(--gradient-main);
            color: white;
            border: none;
            border-radius: 50px;
            font-size: 1rem;
            font-weight: 600;
            cursor: pointer;
            box-shadow: 0 10px 20px rgba(121, 40, 202, 0.3);
            transition: 0.3s;
            text-decoration: none;
            display: inline-block;
        }

        .btn:hover {
            transform: translateY(-3px);
            box-shadow: 0 15px 30px rgba(121, 40, 202, 0.4);
        }

        .hero-img {
            flex: 1;
            z-index: 1;
            position: relative;
            display: flex;
            justify-content: center;
        }

        .hero-img img {
            width: 100%;
            max-width: 500px;
            border-radius: 20px;
            animation: float 6s ease-in-out infinite;
            box-shadow: 20px 20px 0px var(--yellow);
            border: 3px solid var(--dark);
        }

        /* --- Featured Section --- */
        .products {
            padding: 5rem 5%;
        }

        .section-title {
            text-align: center;
            font-size: 2.5rem;
            margin-bottom: 3rem;
        }

        .section-title span {
            border-bottom: 4px solid var(--cyan);
        }

        .grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
            gap: 2.5rem;
        }

        .card {
            background: white;
            border-radius: 20px;
            padding: 1rem;
            box-shadow: 0 10px 40px rgba(0,0,0,0.05);
            transition: 0.3s;
            position: relative;
            border: 1px solid rgba(0,0,0,0.05);
            overflow: hidden;
        }

        .card:hover {
            transform: translateY(-10px);
            box-shadow: 0 20px 50px rgba(0,0,0,0.1);
            border-color: var(--primary);
        }

        .card-badge {
            position: absolute;
            top: 20px;
            left: 20px;
            background: var(--yellow);
            color: var(--dark);
            padding: 5px 10px;
            border-radius: 5px;
            font-weight: 800;
            font-size: 0.8rem;
            z-index: 2;
        }

        .card-img-container {
            height: 300px;
            width: 100%;
            border-radius: 15px;
            overflow: hidden;
            margin-bottom: 1rem;
            background: #eee;
        }

        .card-img-container img {
            width: 100%;
            height: 100%;
            object-fit: cover;
            transition: transform 0.5s;
        }

        .card:hover .card-img-container img {
            transform: scale(1.1);
        }

        .card-info h3 {
            font-size: 1.2rem;
            margin-bottom: 0.5rem;
        }

        .card-info .price {
            font-size: 1.3rem;
            font-weight: 800;
            color: var(--primary);
            margin-bottom: 1rem;
            display: block;
        }

        .add-btn {
            width: 100%;
            padding: 0.8rem;
            background: white;
            border: 2px solid var(--secondary);
            color: var(--secondary);
            border-radius: 10px;
            font-weight: 600;
            cursor: pointer;
            transition: 0.3s;
        }

        .add-btn:hover {
            background: var(--secondary);
            color: white;
        }

        /* --- Footer --- */
        footer {
            background: var(--dark);
            color: white;
            padding: 4rem 5%;
            margin-top: 4rem;
        }

        .footer-content {
            display: flex;
            justify-content: space-between;
            flex-wrap: wrap;
            gap: 2rem;
        }

        .footer-col h4 {
            color: var(--yellow);
            margin-bottom: 1.5rem;
            font-size: 1.2rem;
        }

        .socials a {
            color: white;
            font-size: 1.5rem;
            margin-right: 1rem;
            transition: 0.3s;
        }

        .socials a:hover {
            color: var(--cyan);
        }

        /* --- Toast Notification --- */
        .toast {
            position: fixed;
            bottom: 30px;
            right: 30px;
            background: var(--dark);
            color: white;
            padding: 1rem 2rem;
            border-radius: 10px;
            display: flex;
            align-items: center;
            gap: 10px;
            box-shadow: 0 10px 30px rgba(0,0,0,0.3);
            transform: translateY(100px);
            opacity: 0;
            transition: 0.4s;
            z-index: 2000;
            border-left: 5px solid var(--yellow);
        }

        .toast.show {
            transform: translateY(0);
            opacity: 1;
        }

        /* Mobile Responsive */
        @media (max-width: 768px) {
            .hero {
                flex-direction: column;
                text-align: center;
                padding-top: 2rem;
            }

            .hero-text {
                padding-right: 0;
                margin-bottom: 3rem;
            }

            .hero h1 {
                font-size: 2.8rem;
            }
            
            nav {
                display: none; /* Hide nav links on mobile for simplicity */
            }
        }
    </style>
</head>
<body>

    <!-- Header -->
    <header>
        <div class="logo">Chroma<span style="color:var(--dark)">Style.</span></div>
        <nav>
            <ul>
                <li><a href="#">Home</a></li>
                <li><a href="#shop">Shop</a></li>
                <li><a href="#new">New</a></li>
                <li><a href="#sale">Sale</a></li>
            </ul>
        </nav>
        <div class="cart-icon" onclick="shakeCart()">
            <i class="fas fa-shopping-bag"></i>
            <span class="cart-count" id="cartCount">0</span>
        </div>
    </header>

    <!-- Hero Section -->
    <section class="hero">
        <div class="hero-text">
            <h1>Wear Your <br><span>True Colors</span></h1>
            <p>Discover the boldest collection of 2026. Streetwear that speaks louder than words. Sustainable, vibrant, and uniquely you.</p>
            <a href="#shop" class="btn">Shop The Collection <i class="fas fa-arrow-right"></i></a>
        </div>
        <div class="hero-img">
            <!-- Using Unsplash source for colorful fashion -->
            <img src="https://images.unsplash.com/photo-1523381210434-271e8be1f52b?ixlib=rb-4.0.3&auto=format&fit=crop&w=800&q=80" alt="Colorful Fashion Model">
        </div>
    </section>

    <!-- Products Section -->
    <section class="products" id="shop">
        <h2 class="section-title"><span>Trending Drops</span> 🔥</h2>
        
        <div class="grid">
            <!-- Product 1 -->
            <div class="card">
                <span class="card-badge">NEW</span>
                <div class="card-img-container">
                    <img src="https://images.unsplash.com/photo-1543163521-1bf539c55dd2?ixlib=rb-4.0.3&auto=format&fit=crop&w=800&q=80" alt="Neon Heels">
                </div>
                <div class="card-info">
                    <h3>Electric Blue Kicks</h3>
                    <span class="price">$$125.00$$</span>
                    <button class="add-btn" onclick="addToCart(this)">Add to Cart</button>
                </div>
            </div>

            <!-- Product 2 -->
            <div class="card">
                <span class="card-badge" style="background: var(--accent); color: white;">HOT</span>
                <div class="card-img-container">
                    <img src="https://images.unsplash.com/photo-1576566588028-4147f3842f27?ixlib=rb-4.0.3&auto=format&fit=crop&w=800&q=80" alt="Vibrant Shirt">
                </div>
                <div class="card-info">
                    <h3>Tropical Haze Shirt</h3>
                    <span class="price">$$45.00$$</span>
                    <button class="add-btn" onclick="addToCart(this)">Add to Cart</button>
                </div>
            </div>

            <!-- Product 3 -->
            <div class="card">
                <div class="card-img-container">
                    <img src="https://images.unsplash.com/photo-1539008835657-9e8e9680c956?ixlib=rb-4.0.3&auto=format&fit=crop&w=800&q=80" alt="Velvet Dress">
                </div>
                <div class="card-info">
                    <h3>Midnight Velvet Hoodie</h3>
                    <span class="price">$$89.00$$</span>
                    <button class="add-btn" onclick="addToCart(this)">Add to Cart</button>
                </div>
            </div>

             <!-- Product 4 -->
             <div class="card">
                <span class="card-badge" style="background: var(--secondary); color: white;">-20%</span>
                <div class="card-img-container">
                    <img src="https://images.unsplash.com/photo-1583743814966-8936f5b7be1a?ixlib=rb-4.0.3&auto=format&fit=crop&w=800&q=80" alt="Black Shirt">
                </div>
                <div class="card-info">
                    <h3>Urban Graphite Tee</h3>
                    <span class="price"><s style="color:#aaa; font-size: 0.9rem;">$$40.00$$</s> $$32.00$$</span>
                    <button class="add-btn" onclick="addToCart(this)">Add to Cart</button>
                </div>
            </div>
        </div>
    </section>

    <!-- Footer -->
    <footer>
        <div class="footer-content">
            <div class="footer-col" style="max-width: 300px;">
                <div class="logo" style="margin-bottom: 1rem;">CHROMA.</div>
                <p style="color: #aaa;">Making the world a little more colorful, one outfit at a time. Designed for the bold.</p>
            </div>
            <div class="footer-col">
                <h4>Help</h4>
                <ul style="list-style: none; color: #aaa; line-height: 2;">
                    <li>Shipping Info</li>
                    <li>Returns</li>
                    <li>Sizing Guide</li>
                </ul>
            </div>
            <div class="footer-col">
                <h4>Follow Us</h4>
                <div class="socials">
                    <a href="#"><i class="fab fa-instagram"></i></a>
                    <a href="#"><i class="fab fa-twitter"></i></a>
                    <a href="#"><i class="fab fa-tiktok"></i></a>
                </div>
            </div>
        </div>
        <p style="text-align: center; margin-top: 3rem; color: #555; font-size: 0.9rem;">
            &copy; 2026 ChromaStyle Inc. All rights reserved.
        </p>
    </footer>

    <!-- Toast Notification -->
    <div class="toast" id="toast">
        <i class="fas fa-check-circle" style="color: var(--yellow);"></i>
        <span>Added to cart successfully!</span>
    </div>

    <script>
        // Simple Shopping Cart Logic
        let count = 0;
        const cartCountElement = document.getElementById('cartCount');
        const toast = document.getElementById('toast');
        const cartIcon = document.querySelector('.cart-icon');

        function addToCart(btn) {
            // Update Count
            count++;
            cartCountElement.innerText = count;

            // Animate Button
            const originalText = btn.innerText;
            btn.innerText = "Added!";
            btn.style.background = "var(--secondary)";
            btn.style.color = "white";
            
            // Show Toast
            showToast();

            // Reset Button after 2 seconds
            setTimeout(() => {
                btn.innerText = originalText;
                btn.style.background = "white";
                btn.style.color = "var(--secondary)";
            }, 2000);

            // Shake Cart Icon
            shakeCart();
        }

        function showToast() {
            toast.classList.add('show');
            setTimeout(() => {
                toast.classList.remove('show');
            }, 3000);
        }

        function shakeCart() {
            cartIcon.style.animation = "none";
            cartIcon.offsetHeight; /* trigger reflow */
            cartIcon.style.animation = "pulse-glow 0.5s ease";
        }

        // Smooth Scroll for anchor links
        document.querySelectorAll('a[href^="#"]').forEach(anchor => {
            anchor.addEventListener('click', function (e) {
                e.preventDefault();
                document.querySelector(this.getAttribute('href')).scrollIntoView({
                    behavior: 'smooth'
                });
            });
        });
    </script>
</body>
</html>
